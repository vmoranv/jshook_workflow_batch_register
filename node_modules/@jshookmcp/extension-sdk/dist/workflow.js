export function toolNode(id, toolName, options) {
    return {
        kind: 'tool',
        id,
        toolName,
        input: options?.input,
        retry: options?.retry,
        timeoutMs: options?.timeoutMs,
    };
}
export function sequenceNode(id, steps) {
    return { kind: 'sequence', id, steps };
}
export function parallelNode(id, steps, maxConcurrency = 4, failFast = false) {
    return { kind: 'parallel', id, steps, maxConcurrency, failFast };
}
export function branchNode(id, predicateId, whenTrue, whenFalse, predicateFn) {
    return { kind: 'branch', id, predicateId, predicateFn, whenTrue, whenFalse };
}
//# sourceMappingURL=workflow.js.map